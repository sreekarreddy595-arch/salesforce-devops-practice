public class PracticeRM {
    // This method takes two integers as input and returns their sum
    public static Integer addNumbers(Integer a, Integer b) {
        Integer sum = a + b;
        return sum;
    }
}